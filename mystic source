local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

print("Rayfield loaded")

local Window = Rayfield:CreateWindow({
   Name = "Mystic Falls",
   LoadingTitle = "Loading...",
   LoadingSubtitle = "by Jace",

   Theme = "Amethyst",

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false,

   ConfigurationSaving = {
      Enabled = false,
      FileName = "Light"
   },

   KeySystem = true,
   KeySettings = {
      Title = "CustAms",
      Subtitle = "Nah Riz...",
      Note = ".-.",
      FileName = "ImBackKey",
      SaveKey = false,
      GrabKeyFromSite = false,
      Key = {"Customs"}
   }
})


local MainTab = Window:CreateTab("Main", nil) -- Title, Image
local MainSection = MainTab:CreateSection("Main")


local Keybind = MainTab:CreateKeybind({
   Name = "Motus Spam",
   CurrentKeybind = "F",
   HoldToInteract = false,
   Flag = "Keybind1", -- A flag is the identifier for the configuration file. Make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Keybind)
            local args = {
    "Magic",
    {
        "Motus",
        false
    }
}
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("Witch"):FireServer(unpack(args))
   end,
})

local Button = MainTab:CreateButton({
   Name = "Explosion (K)",
   Callback = function()
            local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local mouse = localPlayer:GetMouse()

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.K then
        local args = {
            "Magic",
            {
                "Explosion",
                mouse.Hit.Position
            }
        }
        ReplicatedStorage:WaitForChild("Events"):WaitForChild("Witch"):FireServer(unpack(args))
        print("Explosion at: " .. tostring(mouse.Hit.Position))
    end
end)
   end,
})

local Button = MainTab:CreateButton({
   Name = "Fire (L)",
   Callback = function()
            local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local mouse = localPlayer:GetMouse()

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.L then
        local targetCharacter = mouse.Target
        
        if targetCharacter then
            -- Find the player that owns this character part
            local targetPlayer = nil
            for _, player in ipairs(Players:GetPlayers()) do
                if player.Character and player.Character:IsDescendantOf(targetCharacter) or targetCharacter:IsDescendantOf(player.Character) then
                    targetPlayer = player
                    break
                end
            end
            
            if targetPlayer and targetPlayer.Character then
                local args = {
                    "Magic",
                    {
                        "FireSpell",
                        targetPlayer.Character
                    }
                }
                ReplicatedStorage:WaitForChild("Events"):WaitForChild("Witch"):FireServer(unpack(args))
                print("Fire Spell cast on: " .. targetPlayer.Name)
            else
                print("No player targeted")
            end
        else
            print("Mouse not pointing at anything")
        end
    end
end)
   end,
})

local Button = MainTab:CreateButton({
   Name = "Invisibility (2)",
   Callback = function()
            local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.Two then
        local args = {
            "Magic",
            {
                "Invisibility"
            }
        }
        ReplicatedStorage:WaitForChild("Events"):WaitForChild("Witch"):FireServer(unpack(args))
        print("Invisibility activated!")
    end
end)
   end,
})

local Button = MainTab:CreateButton({
   Name = "vestus",
   Callback = function()
            local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local localPlayer = Players.LocalPlayer

localPlayer.Chatted:Connect(function(message)
    if message:lower() == "vestus" then
        -- Push all players except yourself
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= localPlayer and player.Character then
                local args = {
                    "Push",
                    {
                        player.Character
                    }
                }
                ReplicatedStorage:WaitForChild("Events"):WaitForChild("Witch"):FireServer(unpack(args))
                print("Pushing " .. player.Name)
            end
        end
    end
end)
   end,
})

local Button = MainTab:CreateButton({
   Name = "Pain Infliction",
   Callback = function()
            local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local mouse = localPlayer:GetMouse()

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.G then
        local targetCharacter = mouse.Target
        
        if targetCharacter then
            -- Find the player that owns this character part
            local targetPlayer = nil
            for _, player in ipairs(Players:GetPlayers()) do
                if player.Character and player.Character:IsDescendantOf(targetCharacter) or targetCharacter:IsDescendantOf(player.Character) then
                    targetPlayer = player
                    break
                end
            end
            
            if targetPlayer and targetPlayer.Character then
                local args = {
                    "PainInflict",
                    {
                        targetPlayer.Character
                    }
                }
                ReplicatedStorage:WaitForChild("Events"):WaitForChild("Witch"):FireServer(unpack(args))
                print("Pain Inflict cast on: " .. targetPlayer.Name)
            else
                print("No player targeted")
            end
        else
            print("Mouse not pointing at anything")
        end
    end
end)
   end,
})

local Button = MainTab:CreateButton({
   Name = "Infinite Yield",
   Callback = function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
   end,
})
